name: student_management_app
description: A student schedule management application
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.2.2
  intl: ^0.19.0
  flutter_local_notifications: ^17.0.0
  permission_handler: ^11.0.1
  timezone: ^0.9.2
  qr_code_scanner: ^1.0.1
  qr_flutter: ^4.1.0
  crypto: ^3.0.3
  cupertino_icons: ^1.0.8
  supabase_flutter: ^2.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true